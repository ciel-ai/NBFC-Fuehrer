# ⚠️ ARCHIVED — DO NOT EDIT

`_ARCHIVE_app/`, `_ARCHIVE_src/` and `_ARCHIVE_expo_root/` are a stale copy
of the mobile app that used to live at the `frontend/` root. They were
superseded by **`frontend/mobile/`** (the canonical Expo app — the only tree
with its own package.json/app.json/eas.json and the newer git history;
verified 2026-07-08: every differing file was newer in frontend/mobile).

Kept for reference only. Safe to delete after 2026-08-08.
